P17
str.assign(str1,start,num)
=> 把str1從start開始取num個字母出來，放到str裡

c++ 切割字串
vector<string>arr;
		
istringstream delim(str);

string temp;

while(getline(delim,temp,' '))
{
	for(int i=0 ; i<temp.size() ;i++)
	{
		if(temp[i] >=65 && temp[i]<=90)
		{
			temp[i] += 32;
		}
	}

	arr.push_back(temp);
}